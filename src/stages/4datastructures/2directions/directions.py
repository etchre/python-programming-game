def move(position, direction):
    # Given a position tuple (row, col) and a direction string,
    # return the new position after moving one step.
    #
    # Directions:
    #   'N' -> row - 1  (up)
    #   'S' -> row + 1  (down)
    #   'E' -> col + 1  (right)
    #   'W' -> col - 1  (left)
    #
    # Hint: create a dictionary mapping each direction to its
    # (row_delta, col_delta), then add the deltas to the position.
    #
    # Example: move((2, 3), 'N') -> (1, 3)
    pass
