import robot

# A `for` loop repeats indented code a set number of times.
# Example — this calls robot.move_right() three times:
#
#     for i in range(3):
#         robot.move_right()
#
# Write a loop that's big enough to reach the goal in every test.
