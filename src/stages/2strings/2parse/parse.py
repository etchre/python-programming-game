def parse_command(command):
    # Parse a command string like "move north 3" into a tuple:
    # (action, direction, amount)
    #
    # Rules:
    # - Commands are case-insensitive (convert to lowercase)
    # - Extra whitespace should be handled (use .split() with no args)
    # - The amount should be returned as an int
    #
    # Example: "  MOVE  North  3  " -> ('move', 'north', 3)
    pass
